#include <stdio.h>
int gcd(int m, int n) {
    if(m<n) {
    int tmp = m;
    m = n;
    n = tmp;
    }
    if(m%n == 0) return n;
    else return gcd(n, m%n);
}
int main() {
    int m, n;
    m = 5;
    n = 8;
    printf("%d", gcd(m, n));
    return 0;
}
